# Environment variables for portfolio-site
# Copy this file to .env.local for local development
# and set values before running `npm run dev`.
# For Gmail SMTP: enable 2FA and create an App Password.

# Required: Gmail address used to send emails
SMTP_USER=

# Required: Gmail App Password (16 chars, no spaces)
SMTP_PASS=

# Required: Destination email to receive notifications.
# If omitted, NOTIFY_EMAIL defaults to SMTP_USER.
NOTIFY_EMAIL=

# Optional: Node environment
NODE_ENV=development